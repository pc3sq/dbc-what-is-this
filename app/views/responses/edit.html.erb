<%= form_for @response, url: response_path(params[:id]) do |f| %>

  <%= f.text_area :content, placeholder: "Content", required: true, autofocus: true %><br>
  <%= f.submit %>

<% end %>

<%= link_to 'Back', :back %>
